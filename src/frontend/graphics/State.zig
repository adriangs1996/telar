const State = @This();
const source_namespace = @import("kitty_delivery.zig");
delete_queue: [source_namespace.graphics.max_placements_per_pane * 2]source_namespace.Delete = undefined,
delete_head: usize = 0,
delete_len: usize = 0,
delete_overflow: bool = false,
next_image_id: u32 = 1,
next_placement_id: u32 = 1,
host_zlib: bool = false,
compression_scheduler: ?source_namespace.CompressionScheduler = null,
pending_compression: ?*source_namespace.Compression = null,
orphan_compression: bool = false,
compression_input: []u8 = &.{},
pass_counter: u64 = 0,
shared_expiries: u8 = 0,
clock_ns: u64 = 0,
retire_latency: source_namespace.diagnostics.Timing = .{},
partial: ?source_namespace.PartialTransmission = null,
