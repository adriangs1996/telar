//! The window the sidebar band must fit, in device pixels: its width, the
//! right padding the grid keeps, the cell width the workbench minimum is
//! counted in and the display scale that turns logical pixels physical.
width: u32,
cell_width: u32,
padding_x: u32 = 0,
scale: f32 = 1,
