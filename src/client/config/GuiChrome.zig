//! Native chrome text preferences. The chrome derives its three text sizes
//! from the terminal font; `scale` multiplies them without touching the
//! cell grid or the band heights.
scale: f32 = 1,
